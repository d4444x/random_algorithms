As the name implies just random algorithm/data structure implementation to refresh my memory on them. NOTE: Hopefully the ideas are right idc too much about speed (as you can tell from the python use).
